import { IToast } from '../interface';
const data: Partial<IToast> = {
    mode: 'success',
    title: 'Título aqui',
    description: 'Hello, world! This is a toast message.'
};

export default {
    data,
};
